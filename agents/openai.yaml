interface:
  display_name: "Codex Windows Fast Patch"
  short_description: "Repair Codex Desktop gates, plugins, Computer Use, and history."
  brand_color: "#2563EB"
  default_prompt: "Use $codex-windows-fast-patch to diagnose and selectively repair Codex Desktop gates, plugins, Computer Use, remote control, or local history on Windows."

policy:
  allow_implicit_invocation: true
